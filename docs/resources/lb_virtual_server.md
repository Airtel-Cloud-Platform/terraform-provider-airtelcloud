---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "airtelcloud_lb_virtual_server Resource - terraform-provider-airtelcloud"
subcategory: ""
description: |-
  Manages a Virtual Server on an Airtel Cloud Load Balancer Service. A virtual server defines L4/L7 load balancing rules with backend nodes.
---

# airtelcloud_lb_virtual_server (Resource)

Manages a Virtual Server on an Airtel Cloud Load Balancer Service. A virtual server defines L4/L7 load balancing rules with backend nodes.

## Example Usage

```terraform
resource "airtelcloud_lb_virtual_server" "http" {
  lb_service_id     = airtelcloud_lb_service.example.id
  name              = "http-vs"
  vip               = airtelcloud_lb_vip.example.fixed_ips
  protocol          = "HTTP"
  port              = 80
  routing_algorithm = "ROUND_ROBIN"
  vpc_id            = "029ac9b8-d93e-4691-a7cb-2f651c607cfe"
  interval          = 30
  monitor_name      = "http-monitor"
  monitor_protocol  = "HTTP"
  x_forwarded_for   = true

  nodes = [
    {
      compute_name = "web-server-1"
      compute_ip   = "10.1.1.10"
      port         = 8080
      weight       = 10
    },
    {
      compute_name = "web-server-2"
      compute_ip   = "10.1.1.11"
      port         = 8080
      weight       = 10
    },
  ]

  timeouts {
    create = "15m"
  }
}
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `interval` (Number) The health check interval in seconds.
- `nodes` (Attributes List) The backend nodes for the virtual server. (see [below for nested schema](#nestedatt--nodes))
- `port` (Number) The listener port number.
- `protocol` (String) The listener protocol (HTTP, HTTPS, TCP, UDP).
- `routing_algorithm` (String) The load balancing algorithm (e.g., ROUND_ROBIN, LEAST_CONNECTIONS).
- `vip` (String) The VIP fixed IP address to bind this virtual server to (e.g. airtelcloud_lb_vip.example.fixed_ips). The provider resolves it to vip_port_id.

### Optional

- `certificate_id` (String) The SSL certificate ID (required for HTTPS protocol).
- `lb_service_id` (String) The ID of the parent LB service. Either lb_service_id or lb_service_name must be specified.
- `lb_service_name` (String) The name of the parent LB service. If set, it is resolved to lb_service_id. Either lb_service_id or lb_service_name must be specified.
- `monitor_name` (String) The name of the health check monitor.
- `monitor_port` (Number) The health check monitor port.
- `monitor_protocol` (String) The health check monitor protocol (HTTP, HTTPS, TCP, UDP).
- `name` (String) The name of the virtual server.
- `persistence_enabled` (Boolean) Whether session persistence is enabled.
- `persistence_type` (String) The session persistence type (cookie or source_ip).
- `pool_name` (String) The name of the backend pool.
- `redirect_https` (Boolean) Whether to redirect HTTP to HTTPS.
- `timeout` (Number) The health check timeout in seconds.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `vpc_id` (String) The VPC ID. Either vpc_id or vpc_name must be specified.
- `vpc_name` (String) The name of the VPC. If set, it is resolved to vpc_id. Either vpc_id or vpc_name must be specified.
- `x_forwarded_for` (Boolean) Whether to add X-Forwarded-For header.

### Read-Only

- `id` (String) The unique identifier of the virtual server.
- `status` (String) The current status of the virtual server.
- `vip_port_id` (Number) The VIP port ID this virtual server is bound to. Computed by resolving the vip fixed IP.

<a id="nestedatt--nodes"></a>
### Nested Schema for `nodes`

Required:

- `compute_ip` (String) The backend instance IP address (sent as resource_ip).
- `port` (Number) The backend port number.

Optional:

- `compute_id` (String) The backend instance ID (UUID). Either compute_id or compute_name must be specified for each node.
- `compute_name` (String) The backend instance name. If set, it is resolved to its ID. Either compute_id or compute_name must be specified for each node.
- `max_conn` (Number) The maximum connections for this node.
- `resource_type` (String) The backend member kind: "compute" (a VM, the default) or "bm" (a baremetal server).
- `weight` (Number) The weight for weighted load balancing.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
